---
import CheckIcon2 from "#assets/icons/CheckIcon2.astro";
import { getAllProducts } from "./products-collection.api";

const products = await getAllProducts();
const productCategories = [
  ...new Set(products.flatMap((p) => p.category)),
].filter(Boolean);

const siteUrl = Astro.site ? Astro.site.toString() : "https://www.alefoods.pe";

const itemListSchema = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  name: "Alefoods Products",
  description: "Premium Peruvian cooked quinoa and export quality coffee.",
  numberOfItems: products.length,
  itemListElement: products.map((product, index) => ({
    "@type": "ListItem",
    position: index + 1,
    item: {
      "@type": "Product",
      name: product.title,
      url: new URL(`/products/${product.slug}/`, siteUrl).toString(),
      image: new URL(product.picture[0]?.url || "", siteUrl).toString(),
      description: product.shortDescription,
      offers: {
        "@type": "Offer",
        priceCurrency: "PEN",
        price: product.price,
      },
    },
  })),
};
---

<script type="application/ld+json" set:html={JSON.stringify(itemListSchema)} />

<section class="px-4 sm:px-6 md:px-8 pb-8 sm:pb-12 md:pb-16">
  <div class="flex flex-col lg:flex-row gap-8 lg:gap-12">
    <aside class="w-full lg:w-64 shrink-0">
      <h2 class="text-lg font-bold text-primary mb-6">Filter by Category</h2>
      <div class="bg-light-bg/50 shadow-sm">
        <ul class="divide-y divide-primary/20">
          <li>
            <button
              type="button"
              data-filter="all"
              class="filter-btn-product w-full text-left px-4 py-3 text-sm border-2 bg-btn-primary border-btn-primary font-semibold text-light-bg transition-colors capitalize cursor-pointer ease-in-out duration-250 hover:border-2 hover:border-filter hover:bg-filter hover:text-light-bg"
            >
              all
            </button>
          </li>
          {
            productCategories.map((category) => (
              <li>
                <button
                  type="button"
                  data-filter={category}
                  class="filter-btn-product w-full text-left px-4 py-3 text-sm border-2 border-btn-primary font-semibold  bg-light-bg text-primary transition-colors capitalize cursor-pointer ease-in-out duration-250 hover:border-2 hover:border-btn-primary hover:bg-filter hover:text-light-bg"
                >
                  {category}
                </button>
              </li>
            ))
          }
        </ul>
      </div>
    </aside>

    <!-- Products Grid -->
    <div class="flex-1">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5 mb-12">
        {
          products.map((product) => (
            <a
              data-category={product.category}
              href={`/products/${product.slug}/`}
              aria-label={`View details of ${product.title}`}
              title={product.title}
              class="product-card group block transition-all duration-300"
            >
              <div class="bg-light-bg/50 rounded-2xl shadow-sm  border border-primary hover:shadow-md transition-shadow hover:bg-light-bg">
                <div class="relative mb-4 aspect-square overflow-hidden rounded-2xl group-hover:border-b-2 group-hover:border-b-primary">
                  <img
                    src={product.picture[0].url}
                    alt={product.picture[0].name || product.title}
                    class="size-full object-cover group-hover:scale-105 transition-transform duration-300 ease-in-out"
                  />
                </div>
                <div class="px-4 pb-5">
                  <div class="flex items-start justify-between mb-2">
                    <h3 class="text-lg sm:text-xl font-bold text-primary  flex-1">
                      {product.title}
                    </h3>
                  </div>
                  <p class="text-sm font-semibold text-secondary mb-4">
                    {product.shortDescription}
                  </p>
                  <div class="flex items-center gap-2 justify-start text-accent">
                    <span class="material-symbols-outlined text-sm ">
                      <CheckIcon2 class={"size-6 text-accent"} />
                    </span>
                    S/.
                    <span class="text-md font-bold">{product.price}.00 </span>
                  </div>
                </div>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </div>
</section>

<script>
  type FilterValue = string;

  function productFunction(): void {
    const filterButtons = document.querySelectorAll<HTMLButtonElement>(
      ".filter-btn-product",
    );
    const cards = document.querySelectorAll<HTMLElement>(".product-card");

    function filterCard(buttonFilterValue: FilterValue): void {
      cards.forEach((card) => {
        const category = card.getAttribute("data-category");
        if (!category) {
          card.classList.add("hidden");
          return;
        }

        if (buttonFilterValue === "all" || buttonFilterValue === category) {
          card.classList.remove("hidden");
        } else {
          card.classList.remove("block");
          card.classList.add("hidden");
        }
      });
    }

    function updateActiveButton(clickedButton: HTMLElement): void {
      filterButtons.forEach((btn) => {
        btn.classList.remove(
          "bg-btn-primary",
          "border-2",
          "border-btn-primary",
          "text-light-bg",
        );
        btn.classList.add("bg-light-bg", "border-btn-primary", "text-primary");
        btn.setAttribute("aria-pressed", "false");
      });

      clickedButton.classList.remove("bg-light-bg", "text-primary");
      clickedButton.classList.add(
        "bg-filter",
        "border-2",
        "border-filter",
        "text-light-bg",
      );
      clickedButton.setAttribute("aria-pressed", "true");
    }

    filterButtons.forEach((button) => {
      button.addEventListener("click", (e: MouseEvent) => {
        const target = e.currentTarget as HTMLButtonElement;
        if (!target) return;

        const buttonFilterValue = target.getAttribute("data-filter");
        if (!buttonFilterValue) return;

        filterCard(buttonFilterValue);
        updateActiveButton(target);
      });
    });

    const allButton = document.querySelector<HTMLButtonElement>(
      '[data-filter="all"]',
    );
    if (allButton) {
      updateActiveButton(allButton);
    }
  }

  productFunction();
  document.addEventListener("astro:page-load", productFunction);
</script>
